name: "Test: Notifications (SMS Provider)"

on:
  workflow_dispatch:
  workflow_call:

jobs:
  test:
    uses: ./.github/workflows/test-run.yml
    with:
      tag: notifications
    secrets: inherit
